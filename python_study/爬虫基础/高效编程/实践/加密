加密index.m3u8文件
下载密钥 放在ts文件夹下


if line.find('URI') != -1: 是一个Python语言中的条件语句。它的意思是：如果字符串 line 中包含子字符串 'URI'，
则执行接下来的代码块。如果 line 中不包含 'URI'，则跳过这个代码块，继续执行后面的代码。

具体来说，line.find('URI') 是查找 line 中第一个出现 'URI' 的位置，如果找到了就返回该位置的索引值（一个非负整数），否则返回-1。
因此，line.find('URI') != -1 的判断结果就是看 'URI' 是否在 line 中出现过。
[] 数组下标、() 圆括号、. 成员选择（对象）、-> 成员选择（指针）1
- 负号运算符、~ 按位取反运算符、++ 自增运算符、-- 自减运算符、* 取值运算符、& 取地址运算符、! 逻辑非运算符、(类型) 强制类型转换、sizeof 长度运算符1
/ 除、* 乘、% 余数（取模）1
+ 加、- 减1
<< 左移、>> 右移1
> 大于、>= 大于等于、< 小于、<= 小于等于1
== 等于、!= 不等于1
& 按位与1
^ 按位异或1
| 按位或1
&& 逻辑与1
|| 逻辑或1
?: 条件运算符1
= 赋值运算符、/= 除后赋值、*= 乘后赋值、%= 取模后赋值、+= 加后赋值、-= 减后赋值、<<= 左移后赋值、>>= 右移后赋值、&= 按位与后赋值、^= 按位异或后赋值、|= 按位或后赋值1